---
title: "Release Note: V1.9.2"
categories:
  - Release
tags:
  - official
---

### Date
Aug 30, 2026

### New Features and Improvements

* XPass Q2 is supported.
  - XPQ2-DPB
* Added new door alarm flags: __LOCK_OVERRIDE__ and __FIRE__. See [AlarmFlag]({{'/api/door/' | relative_url}}#AlarmFlag).
* Added [DESFireConfig.lockEV2SM]({{'/api/card/' | relative_url}}#DESFireConfig) to restrict DESFire cards to EV2 Secure Messaging. See [DESFireLockEV2SM]({{'/api/card/' | relative_url}}#DESFireLockEV2SM).
* Supported graceful shutdown of the gRPC server. The master gateway now provides the __-lr__ option to release the license when it receives SIGTERM/SIGINT. See [the master gateway installation]({{'/master/install/' | relative_url}}).
* Added the [Device Notify API]({{'/api/deviceNotify/' | relative_url}}) to stream device-initiated QR/barcode scans (__BS2_CMD_NOTIFY_QR__). Enable __bypassData__ via [Card.SetQRConfig]({{'/api/card/' | relative_url}}#setqrconfig) and subscribe with [SubscribeQRScan]({{'/api/deviceNotify/' | relative_url}}#SubscribeQRScan).
* The gRPC server now binds to the configured __rpc_server.ip__, so you can select which network interface accepts connections (a specific NIC or `127.0.0.1`). It still defaults to all interfaces. See [the gateway configuration]({{'/gateway/config/' | relative_url}}) and [the master gateway configuration]({{'/master/config/' | relative_url}}).


### Security and CRA

* Each release ships CRA-supporting evidence for every component: a CycloneDX SBOM, a Grype vulnerability report (zero known matches), the `SECURITY.md` policy, and per-archive `SHA256SUMS.txt` checksums. These are included in [the SDK package](https://download.supremainc.com/download-center/pages/login.asp).

### SDK License Agreement

* The SDK License Agreement file (**"LICENSE_AGREEMENT.txt"**) must be in the same path as the current working directory before running device_gateway.
* The SDK License Agreement file (**"LICENSE_AGREEMENT.txt"**) can be created by renaming the file (**"LICENSE.txt"**) distributed with [the SDK package](https://download.supremainc.com/download-center/pages/login.asp).
* Placing the renamed SDK License Agreement file (**"LICENSE_AGREEMENT.txt"**) in the same path as device_gateway is considered as the end user agreeing to the use of the software license.
* The End User License Agreement file (**"EULA.txt"**) is no longer used.